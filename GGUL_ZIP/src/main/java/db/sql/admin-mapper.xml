<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="listCount">
		SELECT COUNT(*) COUNT
		  FROM MEMBER
	</entry>
	<entry key="selectUserList">
		SELECT MEMBER_NO
		      ,MEMBER_ID
		      ,MEMBER_NAME
		      ,MEMBER_NICKNAME
		      ,MEMBER_EMAIL
		      ,MEMBER_ENROLL_DATE
		      ,MEMBER_GRADE
		      ,STATUS
   		  FROM MEMBER
   		 WHERE MEMBER_NO BETWEEN ? AND ?
	</entry>
</properties>